# Creating an Express Server

1. Create a new project folder.
2. Create an index.js file inside the folder.
3. Initialize the project with npm init.
4. Install the Express package using npm install express.
5. Write a simple server application in index.js.
6. Start the server and open it in the browser.
